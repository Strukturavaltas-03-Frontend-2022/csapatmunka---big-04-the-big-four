import { TagToBootstrapClassPipe } from './tag-to-bootstrap-class.pipe';

describe('TagToBootstrapClassPipe', () => {
  it('create an instance', () => {
    const pipe = new TagToBootstrapClassPipe();
    expect(pipe).toBeTruthy();
  });
});
